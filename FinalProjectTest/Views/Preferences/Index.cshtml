@model IEnumerable<FinalProjectTest.Models.Preference>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DietaryRestrictions)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FavoriteCuisines)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AccessibilityRequirements)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DietaryRestrictions)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FavoriteCuisines)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AccessibilityRequirements)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PreferenceID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PreferenceID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PreferenceID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
